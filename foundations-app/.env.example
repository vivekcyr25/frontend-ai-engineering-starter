# Public environment variable examples for Foundations.
# Copy to `.env.local` for local overrides. Never commit real secrets.

# Optional public site URL used later for absolute links / metadata.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Example private server-only variable shape (leave empty until needed).
# HEALTH_UPSTREAM_TOKEN=
