import PlaceholderScreen from "@/components/PlaceholderScreen";

export default function ContactPage() {
  return (
    <PlaceholderScreen
      title="Contact"
      description="Placeholder for contact channels (email, GitHub, LinkedIn) and a form shell. Primary site action remains reviewing technical case studies. No secrets or API keys belong in this route."
      nextHref="/health"
      nextLabel="Open health check"
    />
  );
}
